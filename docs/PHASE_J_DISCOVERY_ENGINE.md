# Discovery Engine

## 1. Discovery Process
`ChangeDiscoveryEngine` investigates:
1. Target intent keywords against `SemanticRepositoryGraph` symbols.
2. Entry points and route decorators (`ROUTES_TO`).
3. Layered architecture patterns (`SERVICE_REPOSITORY`, `API_CONTROLLER`).
4. Attaches machine-readable `Evidence` to identified target files.
